version: '3'

services:
  redis:
    image: redislabs/redismod
    container_name: redis
    ports:
      - "6379:6379"

  web:
    build: .
    container_name: web
    ports: 
      - "5000:5000"
    volumes:
      - .:/glov-api
    depends_on:
      - redis